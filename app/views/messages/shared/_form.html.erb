

<% if controller_path == 'admin/chats' %>
  <%= simple_form_for(@message, url: admin_chat_send_message_path(:url, chat_id: @chat.id), class: 'd-flex', method: :post, remote: true) do |form| %>
    <%= render partial: 'messages/shared/body_form', locals: { form: }%>
  <% end%>
<% else %>
  <%= simple_form_for(@message, url:  support_chat_send_message_path(:url, chat_id: @chat.id), class: 'd-flex', method: :post, remote: true) do |form| %>
    <%= render partial: 'messages/shared/body_form', locals: { form: } %>
  <% end%>
<% end %>
